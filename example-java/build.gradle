apply plugin: 'java'

dependencies {
    compile project(":rxjava-v1")
    compile project(":shield:shield-processor")
    compile project(":shield:shield-maker")
}

sourceCompatibility = "1.8"
targetCompatibility = "1.8"
